# embedding



把人类语言映射到一个 几千维的几何空间。

距离就代表语义相似度 方向代表语义关系

### 对比学习 

contrastive learing

- 训练时 丢给模型一堆正样本和负样本
- 损失函数 会强迫网络在那个高维空间里，**把正样本的向量拼命往一起拉（增加引力），把负样本的向量拼命往外推（增加斥力）**。
- 最终收敛时，这个高维空间就形成了一个完美的语义坐标系。你输入一段 Chunk，它就是这个坐标系里的一个点