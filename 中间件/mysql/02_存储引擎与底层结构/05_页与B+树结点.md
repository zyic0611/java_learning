实际上 一个B+树结点 就是一页。
由于B+树 非叶子结点存储索引 叶子节点存储数据。
并且B+树的高度是越矮越好。

所以实际上一个结点里面存储的是很多索引或者行数据。

对于索引来说 越小越好 这样一个结点里面能存放的索引越多 索引越多 每个索引都有指向别的索引的分支(fan-out) 则该结点的fan-out越多 树高越矮 io效率越高


对于行数据 行数据越小 一个叶结点能放的行越多 这样叶结点就越少 。 叶结点需要上层的非叶子结点的指针来管理 指针也会占用内存大小。 如果叶结点非常多 就需要很多指针来管理 上层非叶子结点塞不下那么多指针 就被迫发生向上分裂 则树高就增加。