# 双亲委派机制

就是向上甩锅，向下兜底。

三种类加载器：

1. 启动类加载器 加载java核心类 加载核心库（`jre/lib/rt.jar`）。
2. 扩展类加载器 java扩展库 加载扩展库（`jre/lib/ext`）
3. 应用类加载器 加载自己写的业务代码 加载用户 ClassPath 下的代码。

## 核心价值

1. 沙箱安全机制 防止核心 API 被用户恶意篡改或替换（如自定义 `java.lang.String` 无效）。
2. 避免重复加载 保证全局唯一性



## Tomcat jdbc数据库连接打破双亲委派机制

