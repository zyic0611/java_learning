# 并发 方法论的选取

核心考量点：性能(吞吐量) 数据一致性 代码难度。

- 唯一索引

一般适用于简单的单表操作，并且用于add，如果update，会遇到并发问题。

- 乐观锁

适用于单表操作，常用于处理并发情况下的update。

- 悲观锁

适用于多表操作，以及复杂业务串行化

- lua+mq

让redis去扛住流量，lua脚本用于一系列在redis里的判断，统一放在lua的原子性操作，减少于java交互，性能高。然后让mq去排队，慢慢处理sql，性能高。

